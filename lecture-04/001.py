salas = float(input('Enter the amunt of sales:'))
com_rate = float(input('Enter the commission rate:'))
commission = salas * com_rate
print('The commission is $ ', format(commission, ',.2f'))

salas = float(input('Enter the amunt of sales:'))
com_rate = float(input('Enter the commission rate:'))
commission = salas * com_rate
print('The commission is $ ', format(commission, ',.2f'))

salas = float(input('Enter the amunt of sales:'))
com_rate = float(input('Enter the commission rate:'))
commission = salas * com_rate
print('The commission is $ ', format(commission, ',.2f'))